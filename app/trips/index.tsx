import { View, Text } from "react-native";

export default function TripsList() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Liste des trajets</Text>
    </View>
  );
}
